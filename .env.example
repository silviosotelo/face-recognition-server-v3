# ========================================
# CONFIGURACIÓN DEL SERVIDOR
# ========================================
PORT=4350
NODE_ENV=development

# ========================================
# BASE DE DATOS
# ========================================
DB_PATH=./database.sqlite
DB_POOL_SIZE=10
DB_TIMEOUT=30000

# ========================================
# ARCHIVOS Y UPLOADS
# ========================================
MAX_FILE_SIZE=50mb
UPLOAD_PATH=./public/uploads
MODELS_PATH=./public/models

# ========================================
# CORS Y SEGURIDAD
# ========================================
ALLOWED_ORIGINS=*

# ========================================
# CACHE
# ========================================
CACHE_ENABLED=true
CACHE_TTL=3600
CACHE_MAX_SIZE=1000

# ========================================
# LOGGING
# ========================================
LOG_LEVEL=info
LOG_FILE=./logs/app.log

# ========================================
# 🎯 CONFIGURACIÓN DE RECONOCIMIENTO FACIAL
# ========================================

# Umbrales de Confianza (0.0 - 1.0)
# Valores más bajos = menos estricto, más matches
# Valores más altos = más estricto, menos falsos positivos
FACE_CONFIDENCE_THRESHOLD=0.42        # Umbral principal para matches (recomendado: 0.38-0.45)
FACE_DETECTION_CONFIDENCE=0.65       # Umbral para detectar rostros (recomendado: 0.6-0.8)
FACE_LANDMARK_CONFIDENCE=0.7         # Umbral para detectar landmarks (recomendado: 0.68-0.75)

# Tamaños de Rostro (en píxeles)
FACE_MIN_SIZE=60                     # Tamaño mínimo de rostro (recomendado: 50-80)
FACE_MAX_SIZE=800                    # Tamaño máximo de rostro (recomendado: 600-1000)

# Configuración de Detección
FACE_INPUT_SIZE_TINY=416            # Resolución para TinyFaceDetector (recomendado: 320/416/512)
FACE_INPUT_SIZE_SSD=512             # Resolución para SsdMobilenet (recomendado: 416/512/608)
FACE_MAX_RESULTS=1                  # Máximo número de rostros a detectar

# Configuración por Tipo de Operación
# REGISTER (Registro - Alta precisión)
REGISTER_CONFIDENCE=0.75            # Más estricto para registros (recomendado: 0.7-0.8)
REGISTER_DETECTION_CONFIDENCE=0.8   # Detección más estricta para registros

# RECOGNIZE (Reconocimiento - Balanceado)
RECOGNIZE_CONFIDENCE=0.42           # Balanceado para reconocimiento diario
RECOGNIZE_DETECTION_CONFIDENCE=0.65 # Detección balanceada

# PRECISE (Precisión máxima - Para casos especiales)
PRECISE_CONFIDENCE=0.35             # Menos estricto pero con validaciones adicionales
PRECISE_DETECTION_CONFIDENCE=0.7    # Detección precisa

# ========================================
# 🚀 CONFIGURACIÓN AVANZADA (OPCIONAL)
# ========================================

# Timeouts y Performance
FACE_DETECTION_TIMEOUT=10000        # Timeout en ms para detección (10 segundos)
FACE_MODEL_LOAD_TIMEOUT=30000       # Timeout para cargar modelos (30 segundos)

# Configuración de Calidad de Imagen
IMAGE_MIN_WIDTH=200                 # Ancho mínimo de imagen
IMAGE_MIN_HEIGHT=200                # Alto mínimo de imagen
IMAGE_MAX_WIDTH=2048                # Ancho máximo de imagen
IMAGE_MAX_HEIGHT=2048               # Alto máximo de imagen
IMAGE_QUALITY=0.9                   # Calidad JPEG (0.1-1.0)

# Configuración de Validación
REQUIRE_LANDMARKS=true              # Requerir landmarks para validación
REQUIRE_EXPRESSIONS=false           # Requerir expresiones (opcional)
VALIDATE_FACE_AREA=true             # Validar área del rostro
VALIDATE_FACE_CLARITY=true          # Validar claridad del rostro

# Configuración de Cache para Reconocimiento
FACE_CACHE_ENABLED=true             # Cache específico para face recognition
FACE_CACHE_TTL=1800                 # 30 minutos de cache para descriptores
FACE_CACHE_MAX_USERS=500            # Máximo usuarios en cache